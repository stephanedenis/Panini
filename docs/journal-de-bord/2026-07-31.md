# Journal de bord — 2026-07-31

## Contexte
Optimisation du workspace Panini pour la machine Hauru (pas de carte FPGA pour l'instant).

## Machine cible
- **OS :** openSUSE, kernel 7.1.4
- **CPU :** 2× Intel Xeon E5-2687W v3 (20 cœurs / 40 threads)
- **RAM :** 125 Go (107 Go disponibles)
- **GPU :** 2× NVIDIA Quadro RTX 5000 (16 Go VRAM, CUDA 13.0)
- **Stockage :** 11 To (/home/stephane/Data13TB), 5.1 To libres
- **Python :** 3.13.14 (système + virtualenv .venv)
- **Node :** v26.4.0

## Actions réalisées

### 🔧 Corrections d'imports
- **`src/cloud/__init__.py`** : Ajout d'un `try/except ImportError` autour de `colab_integrator` (module encore dans `research/`, pas migré — cf. journal du 2026-05-11)
- **`src/modules/dynamic_manager.py`** : Correction de `..interfaces` → `.interfaces` (le double-dot pointait vers `src/` au lieu de `src/modules/`)

### 📦 Dépendances
- **Rust 1.97.1** installé via `rustup` (nécessaire pour Panini-FS)
- **psycopg2-binary 2.9.12** installé dans `.venv` (support PostgreSQL pour registry)
- **typer 0.27.0** installé dans `.venv` (dépendance submodule `orchestration/execution`)
- **Git LFS 3.7.1** installé via `zypper` ✅
- **Docker 29.4.0** déjà présent (daemon actif, user pas dans groupe docker)
- **libopenssl-devel, clang-devel, glibc-devel** installés via `zypper` (compilation Rust)

### 🦀 Panini-FS (Rust) — Remise en état
- **`Cargo.toml` workspace** : ajout des dépendances manquantes (`rust-version`, `homepage`, `documentation`, `keywords`, `categories`, `blake3`, `git2`, `rocksdb`, `lru`, `tantivy`, `pulldown-cmark`, `thiserror`, `anyhow`, `uuid`, `walkdir`, `glob`, `regex`, `once_cell`, `lazy_static`, `http`, `axum`, `tower`, `tower-http`, `async-trait`, `tracing`, `tracing-subscriber`, `whatlang`, `config`, `toml`, `criterion`, `pretty_assertions`, `proptest`)
- **`rocksdb`** : `0.22` → `0.24` (compatibilité GCC 16)
- **`axum`** : ajout feature `multipart`
- **`src/schema/concept.rs`** : migration API `pulldown-cmark` 0.10 → 0.11 (`Tag::Heading` tuple → struct variant, `TagEnd` pour `Event::End`)
- ✅ **Compilation OK** : `panini-core` + `panini-api`

### ⚠️ Points d'attention
- **Docker** : user `stephane` pas dans le groupe `docker` → `sudo usermod -aG docker stephane` à faire
- **OntoWave / Pensine-Web** : submodules en scaffolding, pas de `package.json` à installer pour l'instant

### 🐍 Mises à jour Python 3.13
- **pyproject.toml** : `black` target versions ajoutées py312/py313, `ruff` target-version → py313, `mypy` python_version → 3.13
- **`datetime.utcnow()` → `datetime.now(timezone.utc)`** dans :
  - `src/panini_engine/colab_client.py` (2 occurrences)
  - `src/panini_engine/git_registry.py` (1 occurrence)
  - `src/panini_engine/oauth_manager.py` (5 occurrences, y compris `default=` et `onupdate=`)
  - `tests/conftest.py`, `tests/test_oauth_manager.py`, `tests/test_github_connector.py`, `tests/test_registry_writer_integration.py`

### ✅ Résultats des tests
- **94 tests passent**, 110 warnings (↓ de 239)
- Warnings restants : connexions SQLite non fermées (mineur, tests uniquement)

## État en fin de session
- Workspace cohérent, tous les imports résolus
- Environnement Python 3.13 propre
- Rust opérationnel pour Panini-FS
- GPUs détectés et disponibles (2× RTX 5000)
- Git LFS à installer avec droits admin
