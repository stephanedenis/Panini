# Journal de bord — 19 mai 2026

## Synchronisation des submodules

### Contexte
Suite à la session du 15 mai, tous les submodules avaient été committés localement mais research et filesystem restaient non poussés.

### Décisions et actions

**Sync research** : divergence 2 commits locaux vs 3 distants (tous NIPADA). Résolution par `git merge origin/main -X theirs` — merge propre, aucun conflit. Push réussi.

**Pointeurs submodules parent** : `modules/core/filesystem` et `research` avaient de nouveaux commits → commit de mise à jour des pointeurs dans le repo parent + push `origin/main`.

## Harmonisation politique journal de bord

### Problème identifié
Audit complet des `.github/copilot-instructions.md` à travers les 14 submodules :
- 6 modules correctement alignés (renvoi au Hub parent)
- `modules/core/filesystem` : politique locale divergente — pointait vers `Copilotage/journal/` avec un format de nom différent (`YYYY-MM-DD-<host>-<desc>`) et un pre-commit hook fictif
- 6 modules sans `copilot-instructions.md` : `core/knowledge`, `data/ingestion`, `orchestration/colab`, `orchestration/execution`, `shared/speckit`, `research`

### Décisions
- Corriger filesystem : supprimer la politique locale, conserver les règles spécifiques (Dolt = cache calculé, Ontologie 4 catégories), ajouter le renvoi standard au Hub parent.
- Créer les 6 fichiers manquants avec le même modèle que les 6 modules alignés, adapté au rôle de chaque module.

### Résultat
Tous les 13 submodules disposent désormais d'un `copilot-instructions.md` uniforme qui délègue la politique de journal au Hub parent (`docs/journal-de-bord/YYYY-MM-DD.md`).

## État des travaux

- Tous les submodules synchronisés sur leurs dépôts distants respectifs
- Politique journal unifiée sur l'ensemble de l'écosystème (13 submodules)
- Pointeurs submodules à jour dans le repo parent
